.env and config.json files are needed. 

In order to create them, follow this to complete the necessary information for the project to work:

in the .env file:
ACCESS_TOKEN_SECRET=write-your-token-here


in the config.json file:
{
    "connectionString":"write-your-mongodb-connectionString-here"
}